# .inputrc
#
# This file controls the behavior of line input editing for programs that use
# the GNU Readline library (e.g., bash, interactive Python, GDB).


# Turn off audio cues.
set bell-style none

# Turn on colors for completion options.
set colored-stats on

# Turn off case sensitivity for completion options.
set completion-ignore-case on

# Show completion options instead of ringing the bell when there are multiple
# completion options (i.e., when completion is ambiguous).
set show-all-if-ambiguous on

# Turn on brief highlighting of when closing a parenthesis.
set blink-matching-paren on

# Turn on colored highlighting of typed characters.
set colored-completion-prefix on

