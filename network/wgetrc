# Keep server-provided modification times when available.
timestamping = on

# Avoid climbing above the requested directory during recursive downloads.
no_parent = on

# Bound slow network operations and retry loops.
timeout = 60
tries = 3
retry_connrefused = on

# Use the redirected URL's final path for the output filename.
trust_server_names = on
