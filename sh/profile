# .profile
#
# This file initializes shell-agnostic configurations, enable custom options,
# and sets environment variables. This profile of configurations, options, and
# variables is set for interactive shells upon logging in.


# Set Neovim as the default editor.
export EDITOR=nvim
export VISUAL=nvim

# If Vim is installed, alias `vi` to `vim`.
if [ command -v vim ]; then
    alias vi=vim
fi

# If Neovim is installed, alias `vim` to `nvim`.
if [ command -v nvim ]; then
    alias vim=nvim
fi

# If ripgrep is installed, alias `grep` to `rg`.
if [ command -v rg ]; then
    alias grep=rg
fi

